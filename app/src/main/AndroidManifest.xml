<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app.tts">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TTS">
        <activity android:name=".Wallet"></activity>
        <activity android:name=".History" />
        <activity android:name=".PasswordReset" />
        <activity android:name=".PaymentReceipt" />
        <activity android:name=".DetailsViewRoute" />
        <activity android:name=".DateTimeSchedule" />
        <activity android:name=".EconomyRoute" />
        <activity android:name=".CompaniesSelection" />
        <activity android:name=".ChangePassword" />
        <activity android:name=".UserSettingsUpdate" />
        <activity android:name=".UserSettings" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ConfirmDetails" />
        <activity android:name=".UserDropOff" />
        <activity android:name=".UserPickUp" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity
            android:name=".DashboardActivity"
            android:label="@string/title_activity_dashboard"
            android:theme="@style/Theme.TTS.NoActionBar" />
        <activity android:name=".UserSignUp" />
        <activity android:name=".UserLogin" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
    </application>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

</manifest>